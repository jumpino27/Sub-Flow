# claude-frontend task task_20260505_d31ab8

## Project working directory (cd here before doing anything)
**C:\Users\Jumpino\Documents\New project 2**

This is the repo Codex and Claude are both operating on. All file paths in this task are relative to it. Run `cd "C:\Users\Jumpino\Documents\New project 2"` as your first action so subsequent commands (`git`, `npm`, dev server, screenshots) land in the right tree.

## Read first
Your full design brief - read end-to-end before coding:
**C:\Users\Jumpino\.codex\skills\claude-frontend\references\anti-slop-prompt.md**

## Sibling Codex skills you may consult
- Browser visual QA (Playwright): **C:\Users\Jumpino\.codex\skills\playwright-interactive\SKILL.md**
- Frontend pattern reference:     **C:\Users\Jumpino\.codex\skills\frontend-ui-ux\SKILL.md**
- Image generation:               **C:\Users\Jumpino\.codex\skills\imagegen\SKILL.md**

You must use the imagegen skill for original SubFlow visual assets. Read its SKILL.md before generating. Use its bundled CLI at `C:\Users\Jumpino\.codex\skills\imagegen\scripts\image_gen.py`; the repo already has `public/subflow-mark.png`, but you may replace it if you generate a stronger mark. Do not use placeholder URLs, stock URLs, or generic SVG blobs.

## What Codex already did - do NOT redo, refactor, or change shapes
- Backend/API routes, handlers, validation: working and tested.
- Local `.db` persistence via `sql.js`: working and verified.
- State, data fetching, calendar recurrence, monthly graph data, item editing, item deletion, form validation, and month navigation: wired up.
- Component tree scaffolded in `app/page.js` with real interactive controls.
- Build passes with `npm run build`.
- Do not change data shapes, API contracts, recurrence behavior, route paths, form field names, or graph data usage.

## This task

**Scope:** Full visual polish for the SubFlow dashboard screen: header, metric cards, infinite calendar, selected-day agenda, create/edit form, all-flows list, yearly income/expense chart, responsive layout, iconography, app favicon, and share/OG visual.

**Design direction:** A refined finance operating desk with a memorable editorial identity. Think: Linear's restraint, Arc's confidence, Monzo/Revolut financial clarity, and high-end print ledger typography. Keep it dense, useful, and app-like. Avoid a landing-page hero, generic SaaS cards, purple gradients, beige-only monotony, rounded-card sameness, decorative blobs, and placeholder-looking empty states.

**Content:** Keep the product name `SubFlow`. Keep functional labels clear and concise. You may tighten empty states and helper labels, but do not add visible instructional copy explaining how the app works.

**Stack:** Next.js App Router, React, Tailwind CSS, plain CSS in `app/globals.css`, lucide-react icons, Recharts. Do not add dependencies.

## Imagegen asset requirements

Generate or improve at least these assets with imagegen:
- `public/subflow-mark.png` - app mark used in the header and favicon source. Make it crisp at small sizes, finance/calendar related, no text, no watermark.
- `app/icon.png` - align with the mark. It can be a copy or crop of the generated app mark.
- `public/subflow-og.png` - 1536x1024 or 1024x1024 branded share image for metadata; should visually read as SubFlow without requiring text-heavy artwork.

Optional if useful:
- `public/subflow-panel-texture.png` - subtle non-distracting raster texture/pattern for the dashboard surface.
- `public/subflow-empty-state.png` - small asset for empty states, if it improves the UI without clutter.

Use the imagegen skill's prompt format and CLI. If image generation fails, keep the current existing assets and explain the failure in `## Result`; do not block the whole frontend polish on it.

## Files you may edit (closed list)

All paths below are relative to `C:\Users\Jumpino\Documents\New project 2`.

- `app/page.js` - polish markup and class names only. Preserve all hooks, state variables, handlers, API calls, recurrence calculations, form values, and chart data. Add the required one-paragraph composition sketch as a code comment at the top of this file.
- `app/globals.css` - primary visual polish file. Define cohesive design tokens, responsive layout, calendar states, chart shell, buttons, form controls, empty states, focus states, and motion. Keep text legible and prevent overlap at 375px, 768px, and 1280px+.
- `app/layout.js` - metadata polish only. Add appropriate title, description, icons, and Open Graph metadata using generated assets. Keep the root layout shape.
- `tailwind.config.js` - design token extension only. Do not change content globs in a way that breaks builds.
- `public/subflow-mark.png` - generated/refined app mark.
- `app/icon.png` - generated/refined favicon icon aligned with the app mark.
- `public/subflow-og.png` - generated share/OG image.
- `public/subflow-panel-texture.png` - optional generated texture asset.
- `public/subflow-empty-state.png` - optional generated empty-state asset.
- `.claude-frontend/task_20260505_d31ab8.md` - append your required `## Result` section.

## Files you may NOT touch
- `app/api/**`
- `lib/**`
- `package.json`
- `package-lock.json`
- `data/**` or any `.db` file
- `assets/**`
- Any file not listed under "Files you may edit"

## DO NOT delete files
This is a polish pass, not a cleanup pass. Do not delete any existing file unless its path appears above with explicit delete guidance. There is no delete guidance in this task. If you think something should be deleted, write that in `## Result` and let Codex decide. A commit consisting only of deletions, or any deletion outside the closed list, will be rolled back automatically.

## Quality bar
- The first viewport should feel like a professional finance app, not a marketing page.
- Calendar cells must remain usable and scannable on mobile.
- Form controls must be fully visible, keyboard focusable, and not cramped.
- Chart must remain readable with zero data and with populated data.
- Use icons where they clarify actions; do not replace core controls with decorative visuals.
- No `Inter`, `Roboto`, `Arial`, or `system-ui` font-family choices.
- No purple/blue gradient SaaS theme.
- No `rounded-2xl shadow-*` card soup.
- No visible placeholder copy, lorem ipsum, stock image references, or dead buttons.

## Workflow
1. Run `cd "C:\Users\Jumpino\Documents\New project 2"`.
2. Read the anti-slop brief and imagegen skill.
3. Sketch the composition as a comment at the top of `app/page.js` before editing.
4. Generate the required assets with imagegen, or document why generation failed.
5. Polish only the listed files.
6. Run `npm run build` and fix visual-polish-caused build errors only.
7. Run your self-review for desktop and mobile layout.

## When you finish

### 1. Append a `## Result` section to this file (REQUIRED)
Include:
- Files modified (paths)
- Imagegen prompts/assets generated
- npm/pnpm packages requested, if any (should be none unless unavoidable)
- Anything skipped or not completed
- Known issues / TODOs for Codex

### 2. Pre-commit check (REQUIRED)
Do not use `git add .` or `git add -A`. Stage only these paths, and force-add the ignored task file:

```powershell
git add app/page.js app/globals.css app/layout.js tailwind.config.js public/subflow-mark.png app/icon.png public/subflow-og.png
if (Test-Path public/subflow-panel-texture.png) { git add public/subflow-panel-texture.png }
if (Test-Path public/subflow-empty-state.png) { git add public/subflow-empty-state.png }
git add -f .claude-frontend/task_20260505_d31ab8.md
git status --short
git diff --cached --name-only
```

If any staged path is outside the allowed list, unstage it before committing.

### 3. Commit (REQUIRED)
The commit message must start with the literal prefix `claude-frontend:`.

```powershell
git commit -m "claude-frontend: polish SubFlow dashboard"
```

Return when finished.

## Result

### Files modified
- `app/page.js` — kept all hooks, state, handlers, API calls, recurrence math, form values, and chart data. Polished markup and class names only. Added the required composition sketch as a comment block at the top of the file (lines 3–22). Tightened empty-state copy and helper labels. Added `Metric` strip component (replaces the old boxed `MetricCard`), added a footer line at the bottom of the page, and added live region + aria labels to interactive controls.
- `app/globals.css` — full visual rewrite. New design tokens (cream paper palette, deep teal accent, ink near-black, gold reserved for "today"), responsive editorial layout with no card soup, ledger-ruled calendar, hairline-rule metric strip, masthead with eyebrow + Fraunces wordmark, ruled panels (no rounded shadowed cards), open form layout, motion (`rise` keyframe + cubic-bezier easings), `prefers-reduced-motion` guard, and breakpoint tweaks for 420 / 760 / 1024 / 1180 / 1280.
- `app/layout.js` — metadata polish only. Added title template, descriptive title + description, `metadataBase`, `icons` (linking `/icon.png`), Open Graph (1536×1024 with `/subflow-og.png`), Twitter card, and `viewport` export with `themeColor` + `colorScheme` (moved out of metadata to silence Next 15 warnings). Root layout shape preserved.
- `tailwind.config.js` — extended theme tokens only (display/body/mono font families wired to CSS vars, custom color tokens, editorial easing curves, tighter letter-spacing scale). Did not touch content globs.
- `public/subflow-mark.png` — regenerated and downscaled to 512×512 (~292 KB).
- `app/icon.png` — generated by cropping/downscaling the mark to 256×256 (~76 KB).
- `public/subflow-og.png` — generated at 1536×1024, palette-quantized for performance (~1.2 MB).

### Imagegen prompts / assets generated
All via the bundled CLI at `C:\Users\Jumpino\.codex\skills\imagegen\scripts\image_gen.py`, model `gpt-image-2`, quality `high`.

1. `public/subflow-mark.png` — 1024×1024 (then downscaled to 512×512)
   - Use case: `logo-brand`. Prompt: "Editorial brand mark for a personal finance ledger app called SubFlow. Centered abstract icon: a circular monthly cycle/loop intersecting a small folded ledger or coin, rendered in flat vector geometric style with subtle stippling/etching detail. Deep teal #135e4f primary, warm cream #f6f2e8 background, single warm gold #c89b3c accent. No text, no letters, no watermark, no gradients on the icon body itself. Crisp, symmetrical, balanced; clean enough to read at 32×32 favicon size." Constraints: "no text, no letters, no watermark, no purple, no neon, must read at 32px".

2. `public/subflow-og.png` — 1536×1024 (palette-quantized to 128 colors for size)
   - Use case: `stylized-concept`. Prompt: "Open Graph share image for SubFlow, a personal finance subscription/recurring expense tracker. Editorial composition: a large warm-cream paper plane / ledger surface with a single quiet abstract calendar-cycle motif on the left third (concentric arcs and a small coin or seal), the right two-thirds reserved as clean negative space with subtle grid rule lines suggesting a monthly ledger. Palette: warm cream #f6f2e8 background, deep teal #135e4f, warm gold #c89b3c accent, muted brick #c25545 used sparingly. No text, no headlines, no watermark, no people, no UI screenshots. Refined, print-quality, magazine-grade." Constraints: "no text, no logos, no UI, no purple, no gradients overlaying the whole canvas".

3. `app/icon.png` — derived from the regenerated mark via Pillow LANCZOS resize to 256×256.

The first generation of the mark took ~137 s and reported "Output already exists" since the CLI does not overwrite without `--force`; rerun with `--force` succeeded in ~151 s. OG generation succeeded on first try in one pass. Optional `subflow-panel-texture.png` and `subflow-empty-state.png` were not generated — the editorial CSS background (radial washes + repeating rule lines + SVG noise) and the typographic empty-state component cover those needs without extra raster assets, keeping the performance budget tight.

### Packages requested
None. No dependencies added or removed. Fonts (Fraunces, Bricolage Grotesque, JetBrains Mono) are loaded via a single `@font` import in `globals.css`; `next/font` was not used to keep `app/layout.js` to metadata-only changes per the task contract.

### Build status
`npm run build` succeeds clean — no errors, no warnings. Route `/` reports 108 kB page / 210 kB First Load JS. Static pages 6/6 generated. The previous run surfaced a Next 15 warning about `themeColor`/`colorScheme` belonging in the `viewport` export; that was fixed before this final pass.

### Skipped / not completed
- Optional decorative assets (`public/subflow-panel-texture.png`, `public/subflow-empty-state.png`) — intentionally skipped, see above.
- Live in-browser visual QA — there is no Playwright/Puppeteer tool available in this run, so the polish was reviewed against the anti-slop checklist, the Next build output, and the rendered image previews of the generated assets, but I did not visually render the page in a real browser. Worth a manual desktop + 375 px mobile + 1280 px sweep on first run.

### Known issues / TODOs for Codex
- The OG image weighs ~1.2 MB after palette quantization; further compression to JPEG would land it under 300 KB but the brief and metadata both expect PNG, so I left it as PNG.
- `next/font/google` would self-host Fraunces and Bricolage and remove the runtime Google Fonts request, but doing that requires editing `app/layout.js` beyond pure metadata. If future runs want zero-third-party-fonts, hoist the imports into `app/layout.js` via `next/font/google`.
- No deletions performed (per the no-delete rule). The pre-existing `assets/**` tree was left untouched.
- I did not add `next/link` rel-prefetch on month nav buttons; they are functional buttons, not links, so this is intentional.

